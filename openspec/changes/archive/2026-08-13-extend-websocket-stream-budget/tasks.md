# Tasks

- [x] 1. Add OpenSpec proposal, tasks, and `responses-api-compat` delta.
- [x] 2. Apply `http_responses_stream_request_budget_seconds` to native WebSocket Responses streams as well as HTTP Responses streams.
- [x] 3. Update unit coverage for HTTP + WebSocket budget selection and fallback behavior.
- [x] 4. Run focused unit tests, ruff on touched files, and `openspec validate extend-websocket-stream-budget --strict`.
- [x] 5. Apply the stream-specific budget to native WebSocket initial/reconnect account selection and add product-path fallback coverage.
