# Conversation list metrics

The dashboard conversation list needs the total number of requests and the
elapsed wall-clock time between the first and last request. The list API will
expose precise grouped metrics so the UI can render them without per-row
detail requests. Conversation IDs will be top-aligned for readable wrapped
rows.
