# Tasks

- [x] 1. Create OpenSpec change artifacts (proposal, design, tasks, delta specs for usage-refresh-policy, context) and validate
- [x] 2. Add `AccountRefreshClaim` model to `app/db/models.py` (account_id PK/FK CASCADE, claimed_by, claimed_at, claim_expires_at)
- [x] 3. Add Alembic revision `20260713_040000_add_account_refresh_claims` parented on the current main head `20260712_020000_add_api_key_usage_rollups` (re-parented after the usage-rollup migrations merged to main; see design.md); upgrade creates the table, downgrade drops it; single head
- [x] 4. Add `RefreshClaimCoordinator` (`app/modules/accounts/refresh_claims.py`): conditional-upsert `try_acquire` with RETURNING (pg_insert/sqlite_insert, sqlite_writer_section + SQLITE_BUSY retry), `release`, `current_claim`, process-default accessor; add `AccountsRepository.get_by_id_fresh` (populate_existing)
- [x] 5. Add settings: `token_refresh_claim_ttl_seconds` (default 30, validated >= 2x `token_refresh_timeout_seconds`), `token_refresh_claim_wait_seconds` (default 8.0), `token_refresh_claim_poll_seconds` (default 0.25)
- [x] 6. AuthManager: claim acquire -> post-claim fresh re-read (skip+adopt on rotated fingerprint, refresh with re-read ciphertext) -> update_tokens -> release; loser bounded poll loop with adopt/re-acquire/transient timeout (`RefreshError` transport_error=True, never cached as permanent)
- [x] 7. AuthManager permanent-failure path: `get_by_id_fresh` re-read before persisting; replace unconditional `update_status` with `update_status_if_current` CAS'd on the freshly observed state
- [x] 8. `AccountsRepository.update_tokens`: optional `expected_refresh_token_encrypted` CAS; AuthManager passes the ciphertext it read under the claim; on rowcount 0, re-read and adopt latest without error
- [x] 9. Guardian: per-tick dynamic multi-replica check in `_refresh_once` counting live `bridge_ring_members` heartbeats when leader election is disabled; skip pass + warn on >1
- [x] 10. Unit tests: claim upsert statement compiles for both dialects; settings TTL validation; claimant identity
- [x] 11. Integration tests (two-replica simulation): concurrent race (one upstream call, no REAUTH, sticky intact), loser adoption, crashed-claimant TTL takeover, foreign-claim bounded wait raises transient error not cached by the singleflight, stale-guard regression, update_tokens CAS, guardian dynamic detection
- [x] 12. Route-level regression on the proxy responses harness: upstream 401 with claim pre-held by a foreign claimant -> request fails over transiently, no REAUTH_REQUIRED write, sticky sessions intact, zero upstream token exchanges
- [x] 12a. Streaming retry failover: a transient claim timeout on BOTH the pre-401 proactive freshness check and the post-401 forced refresh MUST exclude the account and fail over, releasing the skipped account's stream lease first; route-level regression (`test_proxy_preflight_claim_timeout_fails_over_and_releases_lease`)
- [x] 12b. WebSocket connect failover: a transient, transport-level claim timeout reaching the WebSocket connect path (pre-401 freshness check and post-401 forced refresh) MUST exclude the account and fail over instead of surfacing a bogus 401 `invalid_api_key`, releasing the skipped account's stream lease first, and only when the request is not pinned to a preferred/required account; route-level regression (`test_backend_responses_websocket_transient_refresh_claim_fails_over_instead_of_401`)
- [x] 12c. Compact-responses freshness-check failover: a transient, transport-level claim timeout raised by the compact preflight `_ensure_fresh_with_budget` MUST release the `response_create` lease, record a transient upstream error, exclude the account, and fail over within the compact account-attempt loop instead of letting the non-permanent `RefreshError` escape as an unhandled server error; a preferred-account-pinned request surfaces a retriable upstream-unavailable error instead of crossing accounts; permanent/non-transport refresh failures keep their prior escalation; route-level regression (`test_compact_preflight_claim_timeout_fails_over_and_releases_lease`)
- [x] 12d. Streaming retry hard-pinned sub-case (pre-stream-open): when a stream turn hard-pinned to a required account (`previous_response_id`/file pin sets `preferred_account_id`, and `require_preferred_account`) hits a transient, transport-level claim timeout on the proactive freshness check before the stream opens, the retry loop MUST NOT fall through to the unconditional reselect (which reselected the same owner without releasing the lease or recording a retryable error and exhausted into a misleading `no_accounts`); it MUST stay on the owner (no crossing), release the pinned account's stream lease, and surface a retryable `upstream_unavailable` promptly; route-level regression (`test_proxy_pinned_stream_claim_timeout_stays_on_owner_and_reports_upstream_unavailable`)
- [x] 12e. Streaming retry hard-pinned sub-case (post-401 forced refresh): the SAME hard-pinned handling MUST also apply on the post-401 recovery attempt — a hard-pinned stream that opens, receives a 401, and then hits a transient, transport-level claim timeout on its forced (`force=True`) refresh MUST NOT fall through to the unconditional `continue` (which only recorded the retryable error for movable requests and otherwise reselected the same owner until exhaustion); it MUST stay on the owner (no crossing), release the pinned account's stream lease, and surface a retryable `upstream_unavailable` promptly; route-level regression (`test_proxy_pinned_stream_post_401_claim_timeout_stays_on_owner_and_reports_upstream_unavailable`)
- [x] 12f. WebSocket connect hard-pinned sub-case: when a WebSocket request hard-pinned to a required account (`require_preferred_account`) hits a transient, transport-level claim timeout on EITHER the connect-path proactive freshness check OR the post-401 forced (`force=True`) refresh, `can_transient_failover` is False (a pinned request must not cross accounts), but the connect path MUST NOT fall through to the terminal 401 `invalid_api_key`; it MUST stay on the owner (no crossing, no permanent mark), release the acquired stream lease, and surface a RETRYABLE `upstream_unavailable` connect failure; permanent/non-transport failures keep the 401; route-level regression (`test_backend_responses_websocket_pinned_transient_refresh_claim_emits_retryable_not_401`)
- [x] 13. Migration coverage: upgrade/downgrade over `20260713_040000` (parent `20260712_020000_add_api_key_usage_rollups`) and single-head assertion
- [x] 14. Run targeted pytest, ruff, and `openspec validate serialize-cross-replica-token-refresh`
- [x] 15. Persistence CAS-miss safety: on a successful-refresh `update_tokens` miss, compare the freshly observed refresh-token fingerprint against the exchanged material; adopt only a genuinely different peer rotation, and retry the CAS (bounded) against the observed ciphertext when the same plaintext was merely re-encrypted, so the freshly issued single-use token is never discarded (unit regressions: re-encryption retry persists our token; peer rotation adopted)
- [x] 16. Persistence CAS-exhaustion safety: when the bounded retries are exhausted without the CAS landing, the rotated tokens were never persisted, so raise a transient (non-permanent, `transport_error=True`, never cached) `RefreshError` instead of returning the success sentinel `None`; the caller then retries the whole refresh rather than releasing the claim and advertising unpersisted material (unit regression: `test_refresh_surfaces_transient_error_when_token_cas_never_persists`)
- [x] 17. Compact transient-claim failover MUST NOT penalize account health: replace the `_handle_stream_error(..., "upstream_unavailable")` call in the compact freshness-check transient-claim failover (which routed through `record_error` and pushed an otherwise-healthy account into backoff for peer-claim contention) with a release + exclude + retained retryable `upstream_unavailable` (no `record_error`), matching the streaming/WebSocket paths; the genuine aiohttp/connect transient path keeps its health accounting; route-level regression (`test_compact_preflight_claim_timeout_does_not_penalize_account_health`)
- [x] 18. Recompute the caller refresh budget after a claim wait: when `_refresh_account_with_claim` wins the claim under a caller `token_refresh_timeout_override`, recompute the remaining budget (caller deadline minus elapsed wait) before the upstream exchange; fail fast with the transient claim-timeout when none remains, otherwise cap the exchange via `push_token_refresh_timeout_override(remaining)` so a long claim wait cannot be followed by a full-budget OAuth exchange that overruns the request deadline and pins the repo session + singleflight entry (unit regressions: `test_claim_wait_then_exchange_caps_to_remaining_budget`, `test_claim_wait_exhausting_budget_fails_fast_without_exchange`)
- [x] 19. Settle pinned compact refresh-claim failures before raising: the compact preflight pinned transient-claim branch (`preferred_account_id is not None`) MUST call `_settle_compact_api_key_usage` before `_raise_proxy_unavailable`, matching the sibling post-401 forced-refresh pinned branch, so a bridge/forwarded pinned compact (`api_key_reservation_override`, `owns_reservation` false) whose refresh claim times out finalizes the API-key reservation instead of leaking held quota; route-level regression (`test_proxy_compact_pinned_preflight_claim_timeout_settles_reservation`)
- [x] 20. Compact post-401 forced-refresh transient-claim failover MUST NOT penalize account health: remove the `_handle_stream_error(..., "upstream_unavailable")` call (which routed through `record_error`) from the post-401 movable transient-claim branch, replacing it with a release + exclude + retained retryable `upstream_unavailable` (no `record_error`), matching the preflight branch and the streaming/WebSocket paths; route-level regression (`test_compact_post_401_forced_refresh_claim_timeout_does_not_penalize_account_health`)
- [x] 21. Streaming retry PERMANENT-refresh failover MUST release the stream lease before failing over: on BOTH the proactive freshness check (pre-401) and the post-401 forced (`force=True`) refresh, when `_ensure_fresh_with_budget` raises a PERMANENT `RefreshError`, the retry loop marked the account permanently failed but fell through to the unconditional `continue` WITHOUT releasing `current_account_lease`, leaving the dead account's stream-concurrency slot held for the entire duration of the replacement stream; release the lease (set to `None`) BEFORE the `continue`, matching the transient branches; route-level regressions (`test_proxy_preflight_permanent_refresh_releases_lease_before_failover`, `test_proxy_post_401_permanent_refresh_releases_lease_before_failover`)
- [x] 22. Claim-loser poll sleep MUST be bounded by the remaining caller budget: in `_refresh_account_with_claim` the foreign-claim polling loop capped the total `wait_seconds` to the caller budget but still slept the full configured poll interval each iteration, so a caller budget smaller than the poll interval let the shielded refresh task overrun the deadline while holding its repo session + singleflight entry; sleep `min(poll_interval, deadline - now)` each iteration and stop/fail-fast with the transient claim timeout when nothing remains (integration regression: `test_claim_poll_sleep_capped_to_remaining_caller_budget`)
- [x] 23. Persistence CAS-exhaustion MUST stay conditional (FINDING 1): the final persistence write MUST remain a compare-and-set against the last observed ciphertext, never an unconditional `write(None)` that drops the ciphertext predicate; on a CAS miss re-read and adopt a genuinely different peer rotation, retry against the freshly observed ciphertext on same-plaintext re-encryption, and on true non-convergence raise the transient `token_persist_conflict` error rather than forcing a write that could clobber a peer rotation landing after the final re-read (unit regressions: `test_refresh_adopts_peer_rotation_at_cas_exhaustion_boundary`, `test_refresh_surfaces_transient_error_when_cas_never_lands`)
- [x] 24. Claim release MUST NOT mask refresh success (FINDING 2): the claim-release in `_refresh_account_with_claim`'s `finally` MUST be wrapped (`_release_claim_quietly`) so a transient DB error releasing the claim is retried briefly then logged and suppressed, never propagated over a successful `_perform_refresh`/adoption return; an exception from the refresh body itself still propagates unchanged (unit regressions: `test_successful_refresh_survives_claim_release_error`, `test_claim_release_error_does_not_mask_refresh_body_error`)
- [x] 25. Permanent-failure status-CAS exhaustion MUST become transient (FINDING P2): when `_handle_permanent_refresh_failure`'s bounded status-downgrade CAS is exhausted by a same-plaintext re-encryption storm (unchanged fingerprint, could not persist `reauth_required`) it MUST NOT return the `None` sentinel that re-raises the ORIGINAL permanent error — which would leave proxy callers to run the unguarded `LoadBalancer.mark_permanent_failure()` and clobber a repaired/rotated account. It MUST instead raise a transient (`status_downgrade_conflict`, `transport_error=True`, non-permanent, never cached) `RefreshError` so the caller retries. A status CAS that SUCCEEDS still marks permanent; a genuinely different peer rotation on re-read is still adopted (unit regression: `test_permanent_failure_status_cas_exhaustion_surfaces_transient_error`)
- [x] 27. Previsible-unary transient-claim failover MUST NOT penalize account health (FINDING P2): `_ensure_previsible_unary_fresh_with_failover` (serving movable previsible-unary requests — thread-goal, codex-control, transcription, file ops) called `_handle_stream_error(..., "upstream_unavailable")` (routing through `record_error`) on the skipped account when its proactive freshness check raised a transient, transport-level claim timeout, pushing an otherwise-healthy account into backoff for pure peer-claim contention. Replace it with an exclude + fail over WITHOUT any health penalty (definitionally transient, so it always fails over rather than gating on the message text), matching the streaming/WebSocket/compact paths; the genuine aiohttp/connect transient branch keeps its `_handle_stream_error` accounting. On strict-pin/exhaustion give-up, raise a claim-contention-marked retryable `upstream_unavailable` (`_raise_proxy_unavailable_for_claim_contention`) that the caller's terminal `_handle_proxy_error` recognizes and skips penalizing, so the last claim-held account is not penalized either; genuine-transport exhaustion keeps its terminal penalty. Route-level regressions (`test_proxy_previsible_unary_claim_timeout_fails_over_without_health_penalty`, `test_proxy_previsible_unary_claim_timeout_exhaustion_reports_upstream_unavailable`)
- [x] 26. Cap token-refresh admission wait by the remaining caller budget (FINDING P2): the claim winner's `_perform_refresh` awaits token-refresh ADMISSION before the OAuth HTTP call, and `WorkAdmissionController` waits up to `proxy_admission_wait_timeout_seconds` for a slot on a saturated semaphore — BEFORE the exchange timeout is armed — so a small-budget caller could hold the cross-replica DB refresh claim for the full admission timeout and block peer replicas. `_refresh_tokens` MUST derive a single monotonic deadline from the caller budget (`token_refresh_timeout_override`) and enforce it on BOTH the admission acquire (`asyncio.wait_for`; fail fast with the transient `refresh_claim_timeout` and release the claim when the budget is exhausted at/before admission) AND the exchange (re-derive the remaining budget after the admission wait so admission + exchange together cannot overrun the caller budget); the whole claim-holding window (poll wait + admission + exchange + release) is thus bounded by the caller budget (unit regression: `test_saturated_admission_fails_fast_and_releases_claim_within_budget`)
- [x] 28. Keep GENUINE refresh transport errors on health accounting (FINDING P2, regression of task 27): task 27's previsible-unary no-penalty change gated on the broad `exc.transport_error` flag, but `refresh_access_token` raises `RefreshError(code="transport_error", transport_error=True)` for a GENUINE OAuth transport failure (the OAuth request itself timing out / its upstream connection failing) — NOT only for internal claim contention. So genuine transport failures stopped putting the broken account into transient backoff and it kept getting reselected. Introduce a precise shared predicate `is_refresh_claim_contention(exc)` (in `app/core/auth/refresh.py`) that recognizes ONLY the cross-replica claim/CAS codes — `refresh_claim_timeout`, `status_downgrade_conflict`, `token_persist_conflict` — and gate the previsible-unary no-penalty / claim-contention-unavailable path on THAT; a genuine `code == "transport_error"` `RefreshError` restores the pre-task-27 behavior (message-gated failover with `_handle_stream_error`/`record_error`, and `_raise_proxy_unavailable_for_account` on strict-pin/exhaustion so its terminal penalty is kept). Sibling audit (streaming retry, compact, websocket): each already avoided a `record_error` penalty for a generic transport error BEFORE this change's task-27-era commits (streaming post-401 `continue`d without penalty; compact preflight let the `RefreshError` escape unhandled and post-401 re-raised the 401; websocket emitted a 401 `invalid_api_key`), so none newly suppressed an existing penalty — they are left as-is. Route-level regressions (`test_proxy_previsible_unary_genuine_transport_error_penalizes_account`, plus the task-27 claim-timeout tests preserved)
- [x] 29. Persistence CAS-exhaustion MUST resolve on decrypted PLAINTEXT (supersedes tasks 16 & 23, ending their oscillation): the two prior findings pulled `_persist_refreshed_tokens` in opposite directions — "don't discard the freshly rotated token" (force-write) vs "don't clobber a genuine peer rotation" (raise/stay conditional). Resolve BOTH by deciding on the DECRYPTED refresh-token PLAINTEXT rather than the non-deterministic ciphertext. When the bounded compare-and-set is exhausted, re-read once more and compare the stored plaintext against the token this attempt exchanged FROM (`_decrypt_refresh_token_plaintext`): (i) same plaintext still stored → no peer rotated (only re-encryption), so FORCE-PERSIST the freshly rotated token with an unconditional `write(None)` (dropping it would leave the DB holding the consumed token → next refresh `invalid_grant`); (ii) genuinely different valid plaintext → a peer rotated → ADOPT it, never overwrite; (iii) plaintext undecryptable/uncomparable → raise the transient `token_persist_conflict` (`transport_error=True`, non-permanent, never cached) so the caller retries. This guarantees the freshly rotated token is never dropped AND a genuine peer rotation is never clobbered (unit regressions: `test_refresh_force_persists_new_token_when_cas_never_lands_on_same_plaintext`, `test_refresh_adopts_peer_rotation_at_cas_exhaustion_boundary`)
- [x] 31. Eliminate the unconditional token write entirely — every persist is a guarded CAS (supersedes tasks 16, 23 & 29, ending the oscillation structurally): the ROOT cause of the round-after-round flip between "don't drop the freshly rotated token" (force-write) and "don't clobber a genuine peer rotation" (stay conditional) was the existence of ANY unconditional write. Task 29's plaintext-based force-persist still left a TOCTOU: a genuine peer rotation landing AFTER the final plaintext-confirming re-read but BEFORE the unconditional `write(None)` was clobbered with a token minted from the already-consumed material. Fix definitively: `_persist_refreshed_tokens` is now a single bounded loop where EVERY persist — including the final/exhaustion persist — is a ciphertext-guarded compare-and-set (`WHERE refresh_token_encrypted == :observed`). The guarded write is atomic, so there is no read→write gap; on a miss, re-read and decide on the DECRYPTED plaintext (`_decrypt_refresh_token_plaintext`): (i) genuinely different valid plaintext → a peer rotated → ADOPT it, never overwrite; (ii) same plaintext (re-encryption noise) → retry the guarded CAS against the freshly observed ciphertext so the freshly rotated token still lands (never dropped); (iii) undecryptable → raise the transient `token_persist_conflict`. When the bounded guarded retries are exhausted → raise the transient `token_persist_conflict` (`transport_error=True`, non-permanent, never cached) so the caller retries the whole refresh, rather than falling back to any unconditional write. There is NO code path that writes tokens without a ciphertext guard (`write`'s parameter is narrowed to `bytes`; the repo's `expected_refresh_token_encrypted` CAS is reused). This closes BOTH horns structurally: (A) not dropped — guarded retry keeps pushing the new token in on same-plaintext; (B) not clobbered — a rotation in the former gap now misses→adopts (unit regressions: `test_refresh_adopts_peer_rotation_landing_in_read_write_gap`, `test_refresh_persists_new_token_when_same_plaintext_reencryption_lands_in_gap`, `test_refresh_raises_transient_when_cas_never_lands_on_same_plaintext_storm`, `test_refresh_adopts_peer_rotation_at_cas_exhaustion_boundary`)
- [x] 30. Extend claim-timeout handling to the remaining forced-refresh surfaces (P2): the file-upload, transcription, and codex-control post-401 forced refreshes route through `_ensure_fresh_with_budget_or_auth_error`, which mapped a transient refresh-CLAIM-CONTENTION timeout onto a penalizable `_raise_proxy_unavailable_for_account` (lumped with genuine transport failures) instead of the unpenalized claim-contention path. Gate on `is_refresh_claim_contention(exc)` BEFORE the `transport_error` branch and raise the unpenalized retryable `upstream_unavailable` (`raise_proxy_unavailable_for_claim_contention`) so all four callers (these three surfaces plus the thread-goal failover) surface a retryable `upstream_unavailable` — never a bogus 401 `invalid_api_key` — for peer claim contention; a genuine OAuth `transport_error` keeps its penalizable `upstream_unavailable`, and a permanent/non-transport failure keeps the 401. Also consolidate the previsible-unary failover's claim-contention/transport/connect branches into the shared `support.failover_after_previsible_refresh_error` helper (behavior-preserving; keeps `service.py` under the architecture line ratchet). Route-level regressions (`test_backend_transcribe_post_401_forced_refresh_claim_timeout_reports_upstream_unavailable`, `test_backend_files_create_post_401_forced_refresh_claim_timeout_reports_upstream_unavailable`, `test_codex_control_post_401_forced_refresh_claim_timeout_reports_upstream_unavailable`) plus a chokepoint unit regression (`test_ensure_fresh_with_budget_or_auth_error_maps_refresh_failures_by_kind`)
- [x] 32. Streaming retry GENUINE transport-error failover MUST release the stream lease before failing over (FINDING P2, sweep of the failover-lease-leak class): on BOTH the proactive freshness check (pre-401) and the post-401 forced (`force=True`) refresh, a non-claim `RefreshError` with `code == "transport_error"` (a genuine OAuth token-refresh timeout / connection failure) is routed into the GENERIC transport-failure failover branch, which recorded the account-health penalty (`_handle_stream_error`) and excluded the account but fell through to `continue` WITHOUT releasing `current_account_lease` — leaving the excluded account's stream-concurrency slot held for the entire duration of the replacement stream. Release the lease (set to `None`) BEFORE the `continue` in both generic branches, matching the claim-contention and permanent-failure branches (tasks 12a/21). Class audit: the compact account-attempt loop already releases the `response_create` lease at the top of the freshness-failure handler and inside `_call_compact`'s `finally` before any 401 branch, and the WebSocket connect loop already releases `selected_stream_lease` on every failover/return path (transient-claim `_WebSocketTransientRefreshFailover`, `failover_next`, terminal, and `BaseException`), so no other failover path holds a dead account's slot — the class is closed. Route-level regressions (`test_proxy_preflight_genuine_transport_error_releases_lease_before_failover` gates the pre-401 branch; `test_proxy_post_401_forced_refresh_genuine_transport_error_releases_lease_before_failover` covers the post-401 sibling)
- [x] 33. Boundary clobber (FINDING B1): `_ensure_chatgpt_account_id` ran an UNCONDITIONAL `update_tokens` (`expected_refresh_token_encrypted=None`) on every `ensure_fresh` for a legacy account missing `chatgpt_account_id` — on the fast no-refresh path, outside any claim/CAS — rewriting the refresh-token ciphertext from the stale selection snapshot and clobbering a concurrent peer rotation with already-consumed material (re-entering the exact clobber horn the persist path forbids). The backfill write is now a ciphertext-guarded compare-and-set (`expected_refresh_token_encrypted=account.refresh_token_encrypted`): a concurrent rotation MISSES (no write, re-derived later); the no-rotation case persists the derived id atomically with the unchanged token material (unit regression: `test_ensure_chatgpt_account_id_backfill_is_ciphertext_guarded`)
- [x] 34. Claim TTL on the DATABASE clock (FINDING B2): `try_acquire`/`build_refresh_claim_upsert` bound each replica's Python `utcnow()` as the stored `claim_expires_at` AND the takeover predicate, so inter-replica clock skew let a skewed replica treat a live claim as expired and steal it (two replicas exchanging the same single-use token). Both the stored expiry and the `claim_expires_at < now` predicate are now evaluated on the DB server clock (PostgreSQL `clock_timestamp()`/`now()`; SQLite in-statement `strftime(..., 'now')`), mirroring the leader-election clock-domain fix; `build_refresh_claim_upsert(dialect_name=...)` returns a dialect-specific `text()` binding `account_id`/`claimed_by`/`ttl` (unit regressions: `test_claim_upsert_uses_database_server_clock_not_bound_python_now`, existing dialect-compile tests)
- [x] 35. Budget-bounded post-exchange persist (FINDING B3): the post-exchange token-persist CAS loop and permanent-failure status-CAS loop run while HOLDING the claim but were bounded only by attempt count, so a contended DB write could loop past the claim TTL and let a peer re-exchange the consumed token. `_perform_refresh`/`_persist_refreshed_tokens`/`_handle_permanent_refresh_failure` now take a monotonic `deadline` (min of claim TTL and remaining caller budget, threaded from `_refresh_account_with_claim`): the first best-effort guarded write always runs, but retries stop at the deadline and surface the transient `token_persist_conflict`/`status_downgrade_conflict` (releasing the claim) rather than looping, so total claim-hold stays within budget + a small fixed release (unit regression: `test_persist_cas_stops_at_deadline_instead_of_looping`)
- [x] 36. Single guarded authority for the permanent-status downgrade (FINDING B4, consolidation): the proxy `LoadBalancer.mark_permanent_failure` → `_persist_state` did an UNGUARDED `update_status` that re-clobbered a peer's ACTIVE/rotated repair back to `reauth_required` (the singleflight-joiner path, whose in-memory object never went through AuthManager's guarded CAS). Consolidated at the root: `mark_permanent_failure` now routes its status write through `_persist_state_if_current` with `expected_refresh_token_encrypted=account.refresh_token_encrypted`, so a peer rotation MISSES instead of clobbering. AuthManager remains the primary authority (its CAS runs first and mutates the object, so the load-balancer write is predicate-skipped as redundant in the single-caller case); a genuine failure still yields exactly one guarded DB downgrade; non-refresh permanent failures keep a guarded write (unit regressions: `test_mark_permanent_failure_guards_status_write_against_peer_rotation`, `test_mark_permanent_failure_downgrades_when_token_matches`)
- [x] 37. Compact preflight settlement sweep (FINDING B5): only the preflight claim-contention branch settled the API-key reservation before raising; the sibling terminal raises — the permanent-`RefreshError` re-raise, the non-retryable transport `_raise_proxy_unavailable`, and the pinned genuine-transport `_raise_proxy_unavailable` — raised WITHOUT settling, leaking the bridge-forwarded reservation (`owns_reservation` false, `compact_responses` sole settler). All preflight terminal raises now call `_settle_compact_api_key_usage` before raising, matching the post-401 block (route-level regressions: `test_proxy_compact_pinned_preflight_transport_error_settles_reservation`, `test_proxy_compact_preflight_permanent_refresh_settles_reservation`)
- [x] 38. Warmup honors the claim-contention taxonomy (FINDING B6): the proxy warmup submit path's `except RefreshError` squashed every non-permanent refresh error into `invalid_api_key`, presenting a healthy account as an auth failure when a peer replica held its refresh claim. It now gates on `is_refresh_claim_contention(exc)` and surfaces a retryable `upstream_unavailable` for claim contention (no health penalty), reserving `invalid_api_key` for permanent and genuine non-contention transport failures, matching the five core proxy request paths (route-level regression: `test_warmup_refresh_claim_contention_surfaces_upstream_unavailable`)
- [x] 39. Root enforcement of the refresh-token clobber class at the DATA layer (supersedes task 33's per-caller guard, closing the class structurally): guarding the two known callers left `AccountsRepository.update_tokens` still able to write `refresh_token_encrypted` UNCONDITIONALLY whenever `expected_refresh_token_encrypted` was omitted, so any other/future caller (e.g. the usage identity/plan sync `_sync_identity_metadata`, which previously wrote token ciphertext from a stale in-memory snapshot with NO CAS predicate) could reopen the clobber. Separated the concerns at the repository: `rotate_tokens` is now the ONLY method that writes access/refresh/id token ciphertext and takes a REQUIRED (non-optional, keyword-only) `expected_refresh_token_encrypted` compare-and-set predicate; a new metadata-only `update_account_metadata` writes identity/plan/workspace fields and STRUCTURALLY cannot touch token material (no parameter for it). Routed every caller: refresh persist (`_write_tokens`) → `rotate_tokens`; the `chatgpt_account_id` backfill (`_ensure_chatgpt_account_id`) and the usage identity/plan/workspace sync (`_sync_identity_metadata`) → `update_account_metadata`. Cascaded the `AccountsRepositoryPort` Protocol, the background repo, and every test double through `uv run ty check` (fully green). Now NO code path can write `refresh_token_encrypted` without a compare-and-set guard, so a concurrent rotation always causes a guarded MISS, never an unconditional overwrite (regressions: `test_rotate_tokens_requires_cas_predicate_no_unguarded_write`, `test_update_account_metadata_cannot_touch_token_material`, `test_ensure_chatgpt_account_id_backfill_never_writes_token_material`)
- [x] 40. Do not retry after dropping rotated tokens (FINDING P1, supersedes task 31's exhaustion tail and commit b444256a's single final attempt): in the pathological same-plaintext storm the exhaustion tail raised a bare transient `token_persist_conflict` and dropped the freshly-rotated token, leaving the DB holding the consumed single-use token — a later blind retry then re-exchanged it into an `invalid_grant`/reauth PERMANENT knockout of a HEALTHY account. Mitigation in `_persist_refreshed_tokens`: when the bounded guarded retries are exhausted OR the deadline cuts the retry loop, run a DEDICATED, small, bounded final-persist retry loop (`_FINAL_PERSIST_MAX_ATTEMPTS` attempts, tiny backoff via `_FINAL_PERSIST_RETRY_BASE_SECONDS`) that is DELIBERATELY SEPARATE from the claim/caller deadline — each attempt a ciphertext-guarded CAS on the freshly re-read ciphertext (ADOPT a genuinely different peer plaintext via `_is_genuine_peer_rotation`, retry a same-plaintext re-encryption against the new ciphertext); it lands within a couple attempts in every realistic case. Only if those dedicated retries are ALL exhausted while the stored material stays the consumed token does it reach a SAFE TERMINAL OUTCOME (`_flag_persist_conflict_reauth`): FAIL CLOSED by flagging the account `REAUTH_REQUIRED` through the SAME ciphertext-guarded status CAS (recoverable, operator-visible — the DB genuinely holds a dead token) rather than dropping the rotation behind a bare transient that a blind retry would knock the account out on; a peer rotation observed while flagging is still ADOPTED (never clobbered), and only if even the guarded status write cannot land through its own bounded budget is the transient `token_persist_conflict` demoted to a last resort. Every write remains a guarded CAS — nothing is clobbered — and a healthy account is never permanently knocked out by this race (unit regressions: `test_refresh_flags_reauth_when_cas_never_lands_on_same_plaintext_storm`, `test_persist_cas_deadline_flags_reauth_when_final_retries_exhaust`, `test_persist_cas_stabilizes_on_second_final_attempt_persists_rotated_token`)
- [x] 41. Waiter honors a prior holder's terminal status before reusing a claimed token (FINDING P2, follow-on of task 40's safe-terminal path): the claim-waiter adoption logic keyed only on the refresh-token fingerprint, so when a prior holder finished by committing a terminal status WITHOUT rotating `refresh_token_encrypted` — a permanent `invalid_grant` downgrade, OR the safe-terminal persist-conflict path (`_flag_persist_conflict_reauth`) that flags `REAUTH_REQUIRED` while leaving the consumed token stored — a waiter that won the released claim reached the fingerprint check, saw the SAME unchanged fingerprint, and re-exchanged the same dead/consumed token AGAIN (a second permanent failure for an account the peer had already removed from rotation), defeating the fail-closed intent. In `_refresh_account_with_claim`'s won-claim branch, after the fresh post-claim re-read, HONOR a terminal status committed by a prior holder: when the FRESH row's refresh-token fingerprint is UNCHANGED from the material the refresh was requested with AND its status is terminal (`REAUTH_REQUIRED`/`DEACTIVATED`), do NOT re-exchange — adopt the terminal row onto the caller object and raise a PERMANENT `RefreshError` (`_terminal_status_refresh_error`, code chosen to map back to the SAME committed status through `account_status_for_permanent_failure`, non-transport) so it fails closed. The check uses the FRESH re-read status/fingerprint (never the stale selection snapshot) and composes with the adopt-vs-exchange logic: a CHANGED fingerprint (a peer genuinely re-authenticated/rotated and repaired the account) is still ADOPTED by the pre-existing fingerprint-differs branch, so a repaired account is never treated as terminal (integration regressions: `test_winner_honors_prior_holder_permanent_reauth_on_unchanged_token`, `test_winner_honors_persist_conflict_reauth_on_unchanged_token`, `test_winner_adopts_peer_rotation_that_repaired_a_reauth_account`)
