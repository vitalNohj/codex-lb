## Why

The Settings page still labels the Claude sidecar configuration section as
`Claude Sidecar`, while the dashboard account cards now present the same
integration as CLIProxyAPI. The settings label should use the same
operator-facing product language.

## What Changes

- Rename the Claude sidecar settings section heading to `CLIProxyAPI Integration`.
- Keep the existing route anchor, setting keys, and API behavior unchanged.

## Impact

- Affects dashboard settings copy only.
- No backend behavior, persistence, or API contract changes.
