# Tasks

- [x] 1. Create OpenSpec change artifacts (proposal, design, tasks, delta specs for ADDED `scheduler-coordination` and MODIFIED `usage-refresh-policy`, capability context) and pass `openspec validate harden-scheduler-leader-election --strict`
- [x] 2. Rewrite `app/core/scheduling/leader_election.py`: dialect detection from the session bind; per-dialect acquire SQL (PostgreSQL uses `now()`/`make_interval` server-side, SQLite binds Python UTC datetimes); rowcount-based win detection replacing the post-commit SELECT; remove both `"sqlite"` URL-substring bypasses
- [x] 3. Wire `renew()` (rowcount == 1, demote on 0); add `release()` (DELETE WHERE id = 1 AND leader_id = :me); add `run_if_leader(fn)` with a heartbeat at `max(1, ttl // 3)` seconds cancelling the body on lease loss or 2 consecutive renew errors
- [x] 4. Convert the 7 gate sites to `run_if_leader` and update their `_LeaderElectionLike` protocols: `app/core/usage/refresh_scheduler.py`, `app/modules/api_keys/reset_scheduler.py`, `app/core/openai/model_refresh_scheduler.py`, `app/modules/sticky_sessions/cleanup_scheduler.py`, `app/modules/quota_planner/scheduler.py`, `app/core/auth/guardian.py`, `app/modules/automations/scheduler.py`
- [x] 5. `app/main.py` lifespan finally-block: release the lease after the last scheduler stops, guarded by try/except + `asyncio.wait_for`
- [x] 6. `app/core/config/settings.py`: `leader_election_enabled` default True; `leader_election_ttl_seconds` default 60 with `Field(ge=5)`
- [x] 7. Fix `openspec/specs/quota-phase-planner/context.md` wording; write the scheduler-coordination capability context (change-level, to move on sync)
- [x] 8. Rewrite `tests/integration/test_multi_replica.py` leader test to assert exactly one winner on SQLite; add takeover-after-release, takeover-after-expiry, and renew-demotion tests using two `LeaderElection` instances over one DB
- [x] 9. Add a scheduler-level two-replica integration test asserting the automations tick body runs exactly once across two concurrently ticking instances
- [x] 10. Add `run_if_leader` lifecycle tests: heartbeat keeps a follower out during a long body; stealing the lease cancels the body within one renew interval; release lets a second instance acquire immediately
- [x] 11. Rewrite `tests/unit/test_leader_election.py` for the new API (rowcount-based, dialect-driven); update scheduler unit-test leader fakes and settings default assertions
- [x] 12. Run targeted pytest, ruff check + format, and `openspec validate` until green
- [x] 13. Review follow-up: time-box each renewal attempt (`asyncio.wait_for`, `ttl / 6`) and demote on a passed local lease deadline so a hung DB cannot extend leadership past the TTL
- [x] 14. Review follow-up: bound the post-cancel await of the gated body to a 5s grace and detach (with logged outcome) bodies draining shielded singleflight refreshes; document the residual overlap and its safety argument in `context.md`
- [x] 15. Review follow-up: narrow the disabled-election contract for Auth Guardian (multi-replica ring without election disables the guardian) and emit an operator-visible warning from `build_auth_guardian_scheduler`
- [x] 16. Review follow-up: make shutdown `release()` drain detached gated bodies for a bounded grace and skip the early lease delete while any is still running, so a follower cannot acquire the lease while the old process may still act as leader
- [x] 17. Review follow-up: enforce the renewal time-box with `asyncio.wait` (not `asyncio.wait_for`) so a renewal whose cancellation cleanup hangs cannot pin the heartbeat past the timeout; abandon the stalled renewal without awaiting its unwind and demote on the time-box deadline
- [x] 18. Review follow-up: track the held lease's local deadline on the shared election so a transient `try_acquire` error from one scheduler tick preserves an already-held, still-valid lease instead of demoting the concurrently-running gated body; only an authoritative non-owner result or a failure after the deadline demotes
- [x] 19. Review follow-up: convert the remaining one-time `try_acquire` gate sites to `run_if_leader` for the guarantee's consistency — `app/core/retention/scheduler.py` and `app/modules/accounts/usage_rollup_scheduler.py` — because a prune or fold backfill can iterate many batches and outlive the lease TTL; update the scheduler-coordination requirement to list them and add unit tests asserting the heartbeat gate (not one-shot acquire)
- [x] 20. Review follow-up (P1): keep renewing the lease on the shutdown-cancel path — the gate now cancels and drains the gated body while the heartbeat stays alive, stopping the heartbeat only after the body exits (bounded by the cancel grace), so a body honouring cancellation slower than the remaining TTL cannot let the DB lease expire while it still runs as leader; add a regression test asserting a renewal fires during the shutdown drain
- [x] 21. Review follow-up (P2): treat a renewal rowcount of 0 as an authoritative lease loss captured before commit and demote even if the commit then raises, so a takeover on a flaky connection is not misread as a transient renewal error; add a regression test with a commit that raises on the stolen renewal
- [x] 22. Holistic hardening: derive the locally tracked lease deadline (in `try_acquire`, `renew`, and the heartbeat) from a monotonic instant captured before the acquire/renewal statement is issued rather than after `commit()`, so the local deadline can never outrun the database `clock_timestamp()` expiry as the clock-domain requirement already asserts — SUPERSEDED by task 24
- [x] 24. Review follow-up (P2): the pre-statement instant of task 22 fixed "outrun" but BACKDATES the deadline when the statement waits on a lock longer than a short TTL (the DB stamps `expires_at` at execution time, so a pre-wait instant seeds an already-expired local deadline on a genuinely fresh lease). Close the clock-domain class at the root: have the acquire/renew statements `RETURNING` the row's remaining lease measured on the database's OWN clock (`expires_at - clock_timestamp()` on PostgreSQL; `expires_at - 'now'` via `julianday` on SQLite) and anchor the local deadline to a monotonic instant captured AFTER the statement returns plus that returned remaining — applied uniformly to `try_acquire`, `renew`, the heartbeat's adopted deadline, and the release-keeper renewal (`_renew_lease_row`) on both backends; add real-SQLite regression tests that a lock-delayed acquire/renewal yields a ~full-remaining deadline (no backdate) and that the deadline does not outrun the DB expiry (invariant holds by construction), and update the clock-domain requirement/scenarios
- [x] 23. Review follow-up (P1): close the cross-scheduler stop-sequence gap at its root cause — add a single process-level lease-renewal keeper (`LeaderElection.start_release_keeper`) started at shutdown-begin in `app/main.py` BEFORE stopping any scheduler and stopped by `release()` (which then owns renewal for its bounded drain), so the DB lease is renewed continuously by one owner from shutdown-begin through the end of the drain and cannot expire while any detached/draining leader-gated body across all schedulers may still act as leader; add a regression test asserting the lease keeps being renewed across a > TTL stop-sequence gap while a detached body drains, and update the scheduler-coordination spec contract + scenario
- [x] 25. CI regression (integration-core-3, run 29305711424): leader election is now default-enabled and performs REAL SQLite acquire/renew/release writes, so the app-lifespan release keeper contended with unrelated integration tests' teardown `DROP TABLE` on the shared single-writer SQLite file, surfacing as `database is locked` and a "Failed to release scheduler leader lease" warning that failed the whole shard at setup/teardown. Disable ambient leader election in `tests/conftest.py` (`CODEX_LB_LEADER_ELECTION_ENABLED=false`, alongside the other background schedulers) so the app lifespan does not run a real keeper during unrelated tests; tests that exercise leader election inject their own enabled settings
- [x] 27. Review follow-up (P2): apply the renewal rowcount-0 fix (task 21) to the acquire path — in `try_acquire`, capture the RETURNING row-presence BEFORE commit; a no-row result is an authoritative non-owner verdict that MUST demote (clear the leadership flag and local deadline) and return False even if the subsequent commit raises on a flaky connection, so it can never fall through to the generic `except` preservation branch and let `run_if_leader` run a second singleton body against a row owned by a different leader. Mirror `renew()`'s exact structure; only a genuine connection error observed BEFORE the authoritative row result may preserve. Add a regression test: acquire returns no row then commit raises → demotes (returns False), does not preserve leadership; update the scheduler-coordination requirement + scenario
- [x] 28. Review follow-up (P2): a heartbeat's LOCAL transient-error demotion (two consecutive renewal errors, or an error at/after its own stale local deadline) must NOT clear the SHARED leadership flag/deadline when a concurrent heartbeat on the same shared election has advanced `self._lease_deadline` past this heartbeat's local deadline — doing so cancels a sibling's still-valid leader work and makes its next `renew()` return `False` even though the DB lease is freshly held. The heartbeat adopts the sibling's fresher shared deadline and keeps renewing (at both the top-of-loop expiry check and the consecutive-error demotion branch); the shared flag is cleared only on an authoritative rowcount-0 loss or a genuinely expired shared deadline. Add a regression test (heartbeat A advances the shared deadline while heartbeat B exhausts the consecutive-error cap → shared `_is_leader` stays True, A's work not cancelled, A's next renew succeeds) and update the scheduler-coordination renewal requirement + scenario
- [x] 26. Robustness (real deployments): make the best-effort shutdown lease writes tolerate transient `database is locked`/`busy` — `release()` and the release-keeper/drain renewal (`_renew_lease_row`) swallow a lock error and log it at debug (release leaves the row to expire after the TTL; renewal retries next cadence) instead of propagating/spamming a warning; non-lock errors keep their warning. Add unit regressions asserting the lock is swallowed at debug on both paths and a non-lock error still warns; update the scheduler-coordination release requirement + scenario
