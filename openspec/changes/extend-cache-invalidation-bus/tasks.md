# Tasks

- [x] 1. Add `NAMESPACE_ACCOUNT_ROUTING` / `NAMESPACE_ACCOUNT_SELECTION` / `NAMESPACE_SETTINGS` constants to `app/core/cache/invalidation.py`
- [x] 2. Harden `CacheInvalidationPoller`: `bump()` retry with backoff returning bool, ERROR log + `codex_lb_cache_invalidation_bump_failures_total{namespace}` on final failure; sync coalescing `request_bump()` with pending set flushed at the top of each poll cycle (failed flushes stay pending); escalate poll failures to WARNING after 3 / ERROR after 10 consecutive with `codex_lb_cache_invalidation_poll_failures_total`; register both metrics in `app/core/metrics/prometheus.py`
- [x] 3. Rework `app/modules/proxy/account_cache.py`: `RoutingAvailabilityCache` (injected session factory; snapshot of account id -> status; unavailable = PAUSED|REAUTH_REQUIRED|DEACTIVATED or id absent once seeded; degrade to local-set semantics when unseeded); keep `mark_/clear_/is_account_routing_unavailable` function API delegating to a module-global instance with local mutation + coalesced `account_routing` bump; add awaited durable-bump variants for API-endpoint paths (pause/reactivate/delete in `accounts/service.py`, `oauth/service.py` re-auth, `settings/api.py` binding reactivation)
- [x] 4. Add `propagate: bool = True` to `AccountSelectionCache.invalidate()` enqueueing an `account_selection` coalesced bump (no call-site edits)
- [x] 5. Add `propagate: bool = True` to `SettingsCache.invalidate()` awaiting a durable `settings` bump
- [x] 6. Wire callbacks in `app/main.py` lifespan next to existing api_key/firewall registrations: `account_routing` -> routing snapshot rebuild, `account_selection` -> selection `invalidate(propagate=False)`, `settings` -> settings `invalidate(propagate=False)`; seed the routing snapshot once at poller start
- [x] 7. Confirm `_http_bridge_session_account_active` needs no code change beyond the new `is_account_routing_unavailable` semantics; pin the no-per-request-DB-read invariant with a comment
- [x] 8. Two-replica integration tests in `tests/integration/test_cache_invalidation_bus.py` (routing mark/clear/delete propagation at the bridge reuse path, selection cache propagation, settings propagation, bump resilience, poll-failure escalation, no-feedback-loop, hot-path zero-DB-query guard)
- [x] 9. Spec deltas (query-caching ADDED x2, account-routing MODIFIED, admin-auth ADDED) + document namespace wiring and convergence bounds in `openspec/specs/query-caching/context.md`; `openspec validate extend-cache-invalidation-bus`
- [x] 10. `uv run pytest tests/integration/test_cache_invalidation_bus.py tests/integration/test_multi_replica.py -q -x` plus closest related suites; `uv run ruff check app tests && uv run ruff format --check app tests`
- [x] 11. Codex review follow-up: re-queue `request_bump` requests that arrive while a flush is awaiting the bump (clear pending marker before awaiting); stop acknowledging a namespace version when its callbacks fail (`refresh_from_db` propagates DB errors, poller retries next cycle; startup seed failure degrades with a warning); regression tests + query-caching spec delta update
- [x] 12. Codex review follow-up: add `CacheInvalidationPoller.initialize()` to seed baseline namespace versions at lifespan startup (before the routing-snapshot refresh and before serving traffic) so a peer bump committed before the first poll is observed as a change instead of acknowledged as baseline; regression test + query-caching spec delta scenario
